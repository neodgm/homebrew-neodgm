# Neodgm Neodgm

## How do I install these formulae?

`brew install neodgm/neodgm/<formula>`

Or `brew tap neodgm/neodgm` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
